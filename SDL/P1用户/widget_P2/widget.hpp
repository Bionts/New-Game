#include "widget.h"

	
		void Gwidget::process(const std::string &a,Uint32 b,Uint32 c){
			Uint32 mai;
				std::string temp;
			for(int i=0;i<=100;i++){
		//		if(ispunct(a[i])){mai++;
				if(mai==b){temp="0";}
				
				else{
			temp=temp+a[i];}
				
				
				}
			
			
			}
	