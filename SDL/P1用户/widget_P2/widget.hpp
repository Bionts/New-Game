#include "widget.h"

	
		char* Gwidget::process( char *a[],int e,int f,int g,Uint32 d){
			char temp[f];
		//	Uint32 mai;
	//			std::string temp;
			for(int i=0;i<=100;i++){
		//		if(ispunct(a[i])){mai++;
	//			if(mai==b){temp="0";}
				
	//			else{
	//		temp=temp+a[i];}
	//			temp=a[g];
				for (int inx=0;inx!=f; ++inx) a[inx]=a[g][inx];
				
				}
		return temp;	
			
			}
	